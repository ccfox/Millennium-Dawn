NRY_norge_mining_decision_cat = {
	NRY_mines_in_oslo = {
		cost = 50
		days_remove = 75

		fire_only_once = yes

		available = {
			NOT = { has_country_flag = NRY_taking_decision }
			owns_state = 32
			controls_state = 32
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision NRY_mines_in_oslo"
			set_country_flag = NRY_taking_decision
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision NRY_mines_in_oslo"
			clr_country_flag = NRY_taking_decision
			if = {
				limit = { controls_state = 32 }
				set_temp_variable = { treasury_change = -10 }
				modify_treasury_effect = yes
				if = {
					limit = { has_tech = excavation12 }
					add_resource = { type = steel amount = 52 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation11 }
					add_resource = { type = steel amount = 47 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation10 }
					add_resource = { type = steel amount = 42 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation9 }
					add_resource = { type = steel amount = 37 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation8 }
					add_resource = { type = steel amount = 32 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation7 }
					add_resource = { type = steel amount = 25 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation6 }
					add_resource = { type = steel amount = 20 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation5 }
					add_resource = { type = steel amount = 18 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation4 }
					add_resource = { type = steel amount = 17 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation3 }
					add_resource = { type = steel amount = 16 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation2 }
					add_resource = { type = steel amount = 12 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation1 }
					add_resource = { type = steel amount = 8 state = 32 }
				}
			}
		}

		ai_will_do = {
			base = 180
		}
	}

	NRY_mines_in_vestlandet = {
		cost = 50
		days_remove = 75

		fire_only_once = yes

		available = {
			NOT = { has_country_flag = NRY_taking_decision }
			owns_state = 30
			controls_state = 30
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision NRY_mines_in_vestlandet"
			set_country_flag = NRY_taking_decision
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision NRY_mines_in_vestlandet"
			clr_country_flag = NRY_taking_decision
			if = {
				limit = { controls_state = 30 }
				set_temp_variable = { treasury_change = -10 }
				modify_treasury_effect = yes
				if = {
					limit = { has_tech = excavation12 }
					add_resource = { type = tungsten amount = 52 state = 30 }
				}
				else_if = {
					limit = { has_tech = excavation11 }
					add_resource = { type = tungsten amount = 47 state = 30 }
				}
				else_if = {
					limit = { has_tech = excavation10 }
					add_resource = { type = tungsten amount = 42 state = 30 }
				}
				else_if = {
					limit = { has_tech = excavation9 }
					add_resource = { type = tungsten amount = 37 state = 30 }
				}
				else_if = {
					limit = { has_tech = excavation8 }
					add_resource = { type = tungsten amount = 32 state = 30 }
				}
				else_if = {
					limit = { has_tech = excavation7 }
					add_resource = { type = tungsten amount = 25 state = 30 }
				}
				else_if = {
					limit = { has_tech = excavation6 }
					add_resource = { type = tungsten amount = 20 state = 30 }
				}
				else_if = {
					limit = { has_tech = excavation5 }
					add_resource = { type = tungsten amount = 18 state = 30 }
				}
				else_if = {
					limit = { has_tech = excavation4 }
					add_resource = { type = tungsten amount = 17 state = 30 }
				}
				else_if = {
					limit = { has_tech = excavation3 }
					add_resource = { type = tungsten amount = 16 state = 30 }
				}
				else_if = {
					limit = { has_tech = excavation2 }
					add_resource = { type = tungsten amount = 12 state = 30 }
				}
				else_if = {
					limit = { has_tech = excavation1 }
					add_resource = { type = tungsten amount = 8 state = 30 }
				}
			}
		}

		ai_will_do = {
			base = 180
		}
	}

	NRY_mines_in_norde_norge = {
		cost = 50
		days_remove = 75

		fire_only_once = yes

		available = {
			NOT = { has_country_flag = NRY_taking_decision }
			owns_state = 31
			controls_state = 31
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision NRY_mines_in_norde_norge"
			set_country_flag = NRY_taking_decision
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision NRY_mines_in_norde_norge"
			clr_country_flag = NRY_taking_decision
			if = {
				limit = { controls_state = 31 }
				set_temp_variable = { treasury_change = -10 }
				modify_treasury_effect = yes
				if = {
					limit = { has_tech = excavation12 }
					add_resource = { type = aluminium amount = 52 state = 31 }
				}
				else_if = {
					limit = { has_tech = excavation11 }
					add_resource = { type = aluminium amount = 47 state = 31 }
				}
				else_if = {
					limit = { has_tech = excavation10 }
					add_resource = { type = aluminium amount = 42 state = 31 }
				}
				else_if = {
					limit = { has_tech = excavation9 }
					add_resource = { type = aluminium amount = 37 state = 31 }
				}
				else_if = {
					limit = { has_tech = excavation8 }
					add_resource = { type = aluminium amount = 32 state = 31 }
				}
				else_if = {
					limit = { has_tech = excavation7 }
					add_resource = { type = aluminium amount = 25 state = 31 }
				}
				else_if = {
					limit = { has_tech = excavation6 }
					add_resource = { type = aluminium amount = 20 state = 31 }
				}
				else_if = {
					limit = { has_tech = excavation5 }
					add_resource = { type = aluminium amount = 18 state = 31 }
				}
				else_if = {
					limit = { has_tech = excavation4 }
					add_resource = { type = aluminium amount = 17 state = 31 }
				}
				else_if = {
					limit = { has_tech = excavation3 }
					add_resource = { type = aluminium amount = 16 state = 31 }
				}
				else_if = {
					limit = { has_tech = excavation2 }
					add_resource = { type = aluminium amount = 12 state = 31 }
				}
				else_if = {
					limit = { has_tech = excavation1 }
					add_resource = { type = aluminium amount = 8 state = 31 }
				}
			}
		}

		ai_will_do = {
			base = 180
		}
	}

	# Unlocks w/Deep Sea Mining Focus
	NRY_deep_sea_mining_in_trondelag = {
		cost = 50
		days_remove = 75

		fire_only_once = yes

		available = {
			NOT = { has_country_flag = NRY_taking_decision }
			owns_state = 542
			controls_state = 542
		}
		visible = {
			has_completed_focus = NRY_deep_sea_expansion
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision NRY_deep_sea_mining_in_trondelag"
			set_country_flag = NRY_taking_decision
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision NRY_deep_sea_mining_in_trondelag"
			clr_country_flag = NRY_taking_decision
			if = {
				limit = { controls_state = 542 }
				set_temp_variable = { treasury_change = -10 }
				modify_treasury_effect = yes
				if = {
					limit = { has_tech = excavation12 }
					add_resource = { type = oil amount = 52 state = 542 }
				}
				else_if = {
					limit = { has_tech = excavation11 }
					add_resource = { type = oil amount = 47 state = 542 }
				}
				else_if = {
					limit = { has_tech = excavation10 }
					add_resource = { type = oil amount = 42 state = 542 }
				}
				else_if = {
					limit = { has_tech = excavation9 }
					add_resource = { type = oil amount = 37 state = 542 }
				}
				else_if = {
					limit = { has_tech = excavation8 }
					add_resource = { type = oil amount = 32 state = 542 }
				}
				else_if = {
					limit = { has_tech = excavation7 }
					add_resource = { type = oil amount = 25 state = 542 }
				}
				else_if = {
					limit = { has_tech = excavation6 }
					add_resource = { type = oil amount = 20 state = 542 }
				}
				else_if = {
					limit = { has_tech = excavation5 }
					add_resource = { type = oil amount = 18 state = 542 }
				}
				else_if = {
					limit = { has_tech = excavation4 }
					add_resource = { type = oil amount = 17 state = 542 }
				}
				else_if = {
					limit = { has_tech = excavation3 }
					add_resource = { type = oil amount = 16 state = 542 }
				}
				else_if = {
					limit = { has_tech = excavation2 }
					add_resource = { type = oil amount = 12 state = 542 }
				}
				else_if = {
					limit = { has_tech = excavation1 }
					add_resource = { type = oil amount = 8 state = 542 }
				}
			}
		}

		ai_will_do = {
			base = 180
		}
	}

	NRY_deep_sea_in_oslo = {
		cost = 50
		days_remove = 75

		fire_only_once = yes

		available = {
			NOT = { has_country_flag = NRY_taking_decision }
			owns_state = 32
			controls_state = 32
		}
		visible = {
			has_completed_focus = NRY_deep_sea_expansion
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision NRY_deep_sea_in_oslo"
			set_country_flag = NRY_taking_decision
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision NRY_deep_sea_in_oslo"
			clr_country_flag = NRY_taking_decision
			if = {
				limit = { controls_state = 32 }
				set_temp_variable = { treasury_change = -10 }
				modify_treasury_effect = yes
				if = {
					limit = { has_tech = excavation12 }
					add_resource = { type = chromium amount = 52 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation11 }
					add_resource = { type = chromium amount = 47 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation10 }
					add_resource = { type = chromium amount = 42 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation9 }
					add_resource = { type = chromium amount = 37 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation8 }
					add_resource = { type = chromium amount = 32 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation7 }
					add_resource = { type = chromium amount = 25 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation6 }
					add_resource = { type = chromium amount = 20 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation5 }
					add_resource = { type = chromium amount = 18 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation4 }
					add_resource = { type = chromium amount = 17 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation3 }
					add_resource = { type = chromium amount = 16 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation2 }
					add_resource = { type = chromium amount = 12 state = 32 }
				}
				else_if = {
					limit = { has_tech = excavation1 }
					add_resource = { type = chromium amount = 8 state = 32 }
				}
			}
		}

		ai_will_do = {
			base = 180
		}
	}
}

NRY_ai_path_category = {

	NRY_ai_rally_the_christian_democrats = {

		icon = generic_decision

		cost = 25

		days_re_enable = 30

		visible = { has_global_flag = NRY_CHRISTIAN_PEOPLES_FOCUS_PATH }

		available = {
			has_civil_war = no
			check_variable = { party_pop_array^14 < 0.55 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision NRY_ai_rally_the_christian_democrats"
			set_temp_variable = { party_index = 14 }
			set_temp_variable = { party_popularity_increase = 0.04 }
			set_temp_variable = { temp_outlook_increase = 0.04 }
			change_relative_party_popularity = yes
		}

		ai_will_do = { base = 100 }
	}

	NRY_ai_christian_democrats_take_the_country = {

		icon = generic_decision

		cost = 150

		days_re_enable = 180

		visible = { has_global_flag = NRY_CHRISTIAN_PEOPLES_FOCUS_PATH }

		available = {
			has_civil_war = no
			neutrality_neutral_conservatism_are_in_power = no
			check_variable = { party_pop_array^14 > 0.5 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision NRY_ai_christian_democrats_take_the_country"
			set_temp_variable = { rul_party_temp = 14 }
			change_ruling_party_effect = yes
		}

		ai_will_do = { base = 100 }
	}

	NRY_ai_rally_the_progress_party = {

		icon = generic_decision

		cost = 25

		days_re_enable = 30

		visible = { has_global_flag = NRY_PROGRESS_FOCUS_PATH }

		available = {
			has_civil_war = no
			check_variable = { party_pop_array^20 < 0.55 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision NRY_ai_rally_the_progress_party"
			set_temp_variable = { party_index = 20 }
			set_temp_variable = { party_popularity_increase = 0.04 }
			set_temp_variable = { temp_outlook_increase = 0.04 }
			change_relative_party_popularity = yes
		}

		ai_will_do = { base = 100 }
	}

	NRY_ai_progress_party_takes_the_country = {

		icon = generic_decision

		cost = 150

		days_re_enable = 180

		visible = { has_global_flag = NRY_PROGRESS_FOCUS_PATH }

		available = {
			has_civil_war = no
			nationalist_right_wing_populists_are_in_power = no
			check_variable = { party_pop_array^20 > 0.5 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision NRY_ai_progress_party_takes_the_country"
			set_temp_variable = { rul_party_temp = 20 }
			change_ruling_party_effect = yes
		}

		ai_will_do = { base = 100 }
	}
}
