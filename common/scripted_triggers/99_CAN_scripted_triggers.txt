# The awarded idea is the authoritative outcome; no parallel flag is kept.
CAN_blackberry_outcome_settled = {
	OR = {
		has_idea = CAN_blackberry_embedded_sovereign
		has_idea = CAN_blackberry_secure_systems_holding
		has_idea = CAN_blackberry_software_pivot
		has_idea = CAN_blackberry_waterloo_holdout
	}
}

# The awarded idea is the authoritative outcome; no parallel flag is kept.
CAN_matrox_outcome_settled = {
	OR = {
		has_idea = CAN_matrox_secure_vision_stack
		has_idea = CAN_matrox_certified_visual_systems
		has_idea = CAN_matrox_sovereign_visual_compute
		has_idea = CAN_matrox_open_graphics_commonwealth
	}
}
