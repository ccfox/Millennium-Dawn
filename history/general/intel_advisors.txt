# Majors define their own LAR intel advisors in common/characters/.
every_possible_country = {
	limit = {
		has_dlc = "La Resistance"
		NOT = {
			OR = {
				original_tag = USA
				original_tag = ENG
				original_tag = GER
				original_tag = FRA
				original_tag = SOV
			}
		}
	}

	generate_character = {
		token_base = generic_intel_head
		advisor = {
			idea_token = advisor
			slot = political_advisor
			allowed = { original_tag = PREV }
			available = {
				has_intelligence_agency = yes
				OR = {
					NOT = { has_country_flag = has_intel_advisor_active }
					has_idea = generic_intel_head_advisor
				}
			}
			on_add = {
				OWNER = { set_country_flag = has_intel_advisor_active }
			}
			on_remove = {
				OWNER = { clr_country_flag = has_intel_advisor_active }
			}
			traits = { head_of_intelligence }
			ai_will_do = { factor = 1 }
		}
	}

	generate_character = {
		token_base = generic_intel_defense
		advisor = {
			idea_token = advisor
			slot = political_advisor
			allowed = { original_tag = PREV }
			available = {
				has_intelligence_agency = yes
				OR = {
					NOT = { has_country_flag = has_intel_advisor_active }
					has_idea = generic_intel_defense_advisor
				}
			}
			on_add = {
				OWNER = { set_country_flag = has_intel_advisor_active }
			}
			on_remove = {
				OWNER = { clr_country_flag = has_intel_advisor_active }
			}
			traits = { expert_code_cracker }
			ai_will_do = { factor = 1 }
		}
	}

	generate_character = {
		token_base = generic_intel_operations
		advisor = {
			idea_token = advisor
			slot = political_advisor
			allowed = { original_tag = PREV }
			available = {
				has_intelligence_agency = yes
				OR = {
					NOT = { has_country_flag = has_intel_advisor_active }
					has_idea = generic_intel_operations_advisor
				}
			}
			on_add = {
				OWNER = { set_country_flag = has_intel_advisor_active }
			}
			on_remove = {
				OWNER = { clr_country_flag = has_intel_advisor_active }
			}
			traits = { spymaster }
			ai_will_do = { factor = 1 }
		}
	}

	generate_character = {
		token_base = generic_intel_crypto
		advisor = {
			idea_token = advisor
			slot = political_advisor
			allowed = { original_tag = PREV }
			available = {
				has_intelligence_agency = yes
				OR = {
					NOT = { has_country_flag = has_intel_advisor_active }
					has_idea = generic_intel_crypto_advisor
				}
			}
			on_add = {
				OWNER = { set_country_flag = has_intel_advisor_active }
			}
			on_remove = {
				OWNER = { clr_country_flag = has_intel_advisor_active }
			}
			traits = { mastermind_code_cracker }
			ai_will_do = { factor = 1 }
		}
	}

	generate_character = {
		token_base = generic_intel_commando
		advisor = {
			idea_token = advisor
			slot = political_advisor
			allowed = { original_tag = PREV }
			available = {
				has_intelligence_agency = yes
				OR = {
					NOT = { has_country_flag = has_intel_advisor_active }
					has_idea = generic_intel_commando_advisor
				}
			}
			on_add = {
				OWNER = { set_country_flag = has_intel_advisor_active }
			}
			on_remove = {
				OWNER = { clr_country_flag = has_intel_advisor_active }
			}
			traits = { spymaster_no_lar }
			ai_will_do = { factor = 1 }
		}
	}
}
