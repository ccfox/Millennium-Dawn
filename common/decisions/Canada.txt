CAN_french_happiness = {

	CAN_federal_transfers_to_quebec = {

		icon = GFX_decision_attract_foreign_investor

		days_remove = 90

		custom_cost_trigger = { check_variable = { treasury > 2.4 } }
		custom_cost_text = cost_2_5

		available = {
			NOT = { country_exists = QUE }
			custom_trigger_tooltip = {
				tooltip = CAN_french_happiness_below_80_tt
				check_variable = { french_happiness_var < 80 }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CAN_federal_transfers_to_quebec"
			set_temp_variable = { treasury_change = -2.5 }
			modify_treasury_effect = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_federal_transfers_to_quebec"
			set_temp_variable = { modify_que_happy = 4 }
			CAN_modify_french_happiness = yes
		}

		ai_will_do = {
			base = 10
			modifier = {
				factor = 3
				check_variable = { french_happiness_var < 40 }
			}
			modifier = {
				factor = 0
				has_active_mission = bankruptcy_incoming_collapse
			}
		}
	}

	CAN_bilingual_public_services = {

		icon = GFX_decision_generic_political_discourse

		cost = 60

		days_remove = 120

		available = {
			NOT = { country_exists = QUE }
			custom_trigger_tooltip = {
				tooltip = CAN_french_happiness_below_80_tt
				check_variable = { french_happiness_var < 80 }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_bilingual_public_services"
			add_stability = 0.02
			set_temp_variable = { modify_que_happy = 3 }
			CAN_modify_french_happiness = yes
		}

		ai_will_do = {
			base = 10
			modifier = {
				factor = 3
				check_variable = { french_happiness_var < 40 }
			}
		}
	}
}

CAN_provincial_infrastructure = {
	##Canada "Improve" Infrastructure decisions
	CAN_improve_infrastructure_759 = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 80
		custom_cost_trigger = { check_variable = { treasury > 2.4 } }
		custom_cost_text = cost_2_5

		visible = {
			759 = {
				free_building_slots = {
					building = infrastructure
					size > 0
				}
			}
		}

		available = {
			has_full_control_of_state = 759
			owns_state = 759
		}

		on_map_mode = map_and_decisions_view
		highlight_states = { highlight_state_targets = { state = 759 } }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_improve_infrastructure_759"
			759 = {
				add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}

		ai_will_do = { base = 1 }
	}

	CAN_improve_infrastructure_760 = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 80
		custom_cost_trigger = { check_variable = { treasury > 2.4 } }
		custom_cost_text = cost_2_5

		visible = {
			760 = {
				free_building_slots = {
					building = infrastructure
					size > 0
				}
			}
		}

		available = {
			has_full_control_of_state = 760
			owns_state = 760
		}

		on_map_mode = map_and_decisions_view
		highlight_states = { highlight_state_targets = { state = 760 } }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_improve_infrastructure_760"
			760 = {
				add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}

		ai_will_do = { base = 1 }
	}

	CAN_improve_infrastructure_761 = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 80
		custom_cost_trigger = { check_variable = { treasury > 2.4 } }
		custom_cost_text = cost_2_5

		visible = {
			761 = {
				free_building_slots = {
					building = infrastructure
					size > 0
				}
			}
		}

		available = {
			has_full_control_of_state = 761
			owns_state = 761
		}

		on_map_mode = map_and_decisions_view
		highlight_states = { highlight_state_targets = { state = 761 } }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_improve_infrastructure_761"
			761 = {
				add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}

		ai_will_do = { base = 1 }
	}

	CAN_improve_infrastructure_762 = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 80
		custom_cost_trigger = { check_variable = { treasury > 2.4 } }
		custom_cost_text = cost_2_5

		visible = {
			762 = {
				free_building_slots = {
					building = infrastructure
					size > 0
				}
			}
		}

		available = {
			has_full_control_of_state = 762
			owns_state = 762
		}

		on_map_mode = map_and_decisions_view
		highlight_states = { highlight_state_targets = { state = 762 } }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_improve_infrastructure_762"
			762 = {
				add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}

		ai_will_do = { base = 1 }
	}

	CAN_improve_infrastructure_763 = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 80
		custom_cost_trigger = { check_variable = { treasury > 2.4 } }
		custom_cost_text = cost_2_5

		visible = {
			763 = {
				free_building_slots = {
					building = infrastructure
					size > 0
				}
			}
		}

		available = {
			has_full_control_of_state = 763
			owns_state = 763
		}

		on_map_mode = map_and_decisions_view
		highlight_states = { highlight_state_targets = { state = 763 } }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_improve_infrastructure_763"
			763 = {
				add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}

		ai_will_do = { base = 1 }
	}

	CAN_improve_infrastructure_757 = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 80
		custom_cost_trigger = { check_variable = { treasury > 2.4 } }
		custom_cost_text = cost_2_5

		visible = {
			757 = {
				free_building_slots = {
					building = infrastructure
					size > 0
				}
			}
		}

		available = {
			has_full_control_of_state = 757
			owns_state = 757
		}

		on_map_mode = map_and_decisions_view
		highlight_states = { highlight_state_targets = { state = 757 } }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_improve_infrastructure_757"
			757 = {
				add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}

		ai_will_do = { base = 1 }
	}

	CAN_improve_infrastructure_756 = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 80
		custom_cost_trigger = { check_variable = { treasury > 2.4 } }
		custom_cost_text = cost_2_5

		visible = {
			756 = {
				free_building_slots = {
					building = infrastructure
					size > 0
				}
			}
		}

		available = {
			has_full_control_of_state = 756
			owns_state = 756
		}

		on_map_mode = map_and_decisions_view
		highlight_states = { highlight_state_targets = { state = 756 } }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_improve_infrastructure_756"
			756 = {
				add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}

		ai_will_do = { base = 1 }
	}

	CAN_improve_infrastructure_755 = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 80
		custom_cost_trigger = { check_variable = { treasury > 2.4 } }
		custom_cost_text = cost_2_5

		visible = {
			755 = {
				free_building_slots = {
					building = infrastructure
					size > 0
				}
			}
		}

		available = {
			has_full_control_of_state = 755
			owns_state = 755
		}

		on_map_mode = map_and_decisions_view
		highlight_states = { highlight_state_targets = { state = 755 } }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_improve_infrastructure_755"
			755 = {
				add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}

		ai_will_do = { base = 1 }
	}

	CAN_improve_infrastructure_754 = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 80
		custom_cost_trigger = { check_variable = { treasury > 2.4 } }
		custom_cost_text = cost_2_5

		visible = {
			754 = {
				free_building_slots = {
					building = infrastructure
					size > 0
				}
			}
		}

		available = {
			has_full_control_of_state = 754
			owns_state = 754
		}

		on_map_mode = map_and_decisions_view
		highlight_states = { highlight_state_targets = { state = 754 } }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_improve_infrastructure_754"
			754 = {
				add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}

		ai_will_do = { base = 1 }
	}

	CAN_improve_infrastructure_753 = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 80
		custom_cost_trigger = { check_variable = { treasury > 2.4 } }
		custom_cost_text = cost_2_5

		visible = {
			753 = {
				free_building_slots = {
					building = infrastructure
					size > 0
				}
			}
		}

		available = {
			has_full_control_of_state = 753
			owns_state = 753
		}

		on_map_mode = map_and_decisions_view
		highlight_states = { highlight_state_targets = { state = 753 } }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_improve_infrastructure_753"
			753 = {
				add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}

		ai_will_do = { base = 1 }
	}

	CAN_improve_infrastructure_758 = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 80
		custom_cost_trigger = { check_variable = { treasury > 2.4 } }
		custom_cost_text = cost_2_5

		visible = {
			758 = {
				free_building_slots = {
					building = infrastructure
					size > 0
				}
			}
		}

		available = {
			has_full_control_of_state = 758
			owns_state = 758
		}

		on_map_mode = map_and_decisions_view
		highlight_states = { highlight_state_targets = { state = 758 } }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_improve_infrastructure_758"
			758 = {
				add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}

		ai_will_do = { base = 1 }
	}

	CAN_improve_infrastructure_1159 = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 80
		custom_cost_trigger = { check_variable = { treasury > 2.4 } }
		custom_cost_text = cost_2_5

		visible = {
			1159 = {
				free_building_slots = {
					building = infrastructure
					size > 0
				}
			}
		}

		available = {
			has_full_control_of_state = 1159
			owns_state = 1159
		}

		on_map_mode = map_and_decisions_view
		highlight_states = { highlight_state_targets = { state = 1159 } }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_improve_infrastructure_1159"
			1159 = {
				add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}

		ai_will_do = { base = 1 }
	}

	CAN_improve_infrastructure_1160 = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 80
		custom_cost_trigger = { check_variable = { treasury > 2.4 } }
		custom_cost_text = cost_2_5

		visible = {
			1160 = {
				free_building_slots = {
					building = infrastructure
					size > 0
				}
			}
		}

		available = {
			has_full_control_of_state = 1160
			owns_state = 1160
		}

		on_map_mode = map_and_decisions_view
		highlight_states = { highlight_state_targets = { state = 1160 } }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_improve_infrastructure_1160"
			1160 = {
				add_building_construction = {
					type = infrastructure
					level = 1
				}
			}
		}

		ai_will_do = { base = 1 }
	}
}

CAN_canadian_subsidies_projects = {
	# set_variable = { CAN_stability_factor_var = 0 }
	# set_variable = { CAN_industrial_capacity_factory_var = 0 }
	# set_variable = { CAN_production_speed_buildings_var = 0 }
	# set_variable = { CAN_production_speed_infrastructure_factor_var = 0 }
	# set_variable = { CAN_production_oil_var = 0 }
	# set_variable = { CAN_subsidy_total = 0 }
	CAN_subsidize_military_industry = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 60
		days_re_enable = 1
		cost = 50

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_subsidize_military_industry"
			set_temp_variable = { subsidy_change_tmp = 0.2 }
			CAN_modify_subsidy_changes = yes
			custom_effect_tooltip = { localization_key = modifies_dynamic_modifier_tt MODIFIER = CAN_canadian_economy_dynamic_modifier }
			add_to_variable = { CAN_industrial_capacity_factory_var = 0.05 tooltip = industrial_capacity_factory_tt }
		}

		ai_will_do = { base = 1 }
	}

	CAN_reduce_military_subsidies = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 60
		days_re_enable = 1
		cost = 50

		available = {
			custom_trigger_tooltip = {
				tooltip = CAN_subsidy_total_above_20_tt
				check_variable = { CAN_subsidy_total > 0.2 }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_reduce_military_subsidies"
			set_temp_variable = { subsidy_change_tmp = -0.2 }
			CAN_modify_subsidy_changes = yes
			custom_effect_tooltip = { localization_key = modifies_dynamic_modifier_tt MODIFIER = CAN_canadian_economy_dynamic_modifier }
			add_to_variable = { CAN_industrial_capacity_factory_var = -0.05 tooltip = industrial_capacity_factory_tt }
		}

		ai_will_do = { base = 1 }
	}

	CAN_construction_subsidies = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 60
		days_re_enable = 1
		cost = 50

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_construction_subsidies"
			set_temp_variable = { subsidy_change_tmp = 0.1 }
			CAN_modify_subsidy_changes = yes
			custom_effect_tooltip = { localization_key = modifies_dynamic_modifier_tt MODIFIER = CAN_canadian_economy_dynamic_modifier }
			add_to_variable = { CAN_production_speed_buildings_var = 0.05 tooltip = production_speed_buildings_factor_tt }
		}

		ai_will_do = { base = 1 }
	}

	CAN_reduce_construction_subsidies = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 60
		days_re_enable = 1
		cost = 50

		available = {
			custom_trigger_tooltip = {
				tooltip = CAN_subsidy_total_above_10_tt
				check_variable = { CAN_subsidy_total > 0.1 }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_reduce_construction_subsidies"
			set_temp_variable = { subsidy_change_tmp = -0.1 }
			CAN_modify_subsidy_changes = yes
			custom_effect_tooltip = { localization_key = modifies_dynamic_modifier_tt MODIFIER = CAN_canadian_economy_dynamic_modifier }
			add_to_variable = { CAN_production_speed_buildings_var = -0.05 tooltip = production_speed_buildings_factor_tt }
		}

		ai_will_do = { base = 1 }
	}

	CAN_infrastructure_subsidies = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 60
		days_re_enable = 1
		cost = 50

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_infrastructure_subsidies"
			set_temp_variable = { subsidy_change_tmp = 0.1 }
			CAN_modify_subsidy_changes = yes
			custom_effect_tooltip = { localization_key = modifies_dynamic_modifier_tt MODIFIER = CAN_canadian_economy_dynamic_modifier }
			add_to_variable = { CAN_production_speed_infrastructure_factor_var = 0.05 tooltip = production_speed_infrastructure_factor_tt }
		}

		ai_will_do = { base = 1 }
	}

	CAN_reduce_infrastructure_subsidies = {
		icon = GFX_decision_attract_foreign_investor
		days_remove = 60
		days_re_enable = 1
		cost = 50

		available = {
			custom_trigger_tooltip = {
				tooltip = CAN_subsidy_total_above_10_tt
				check_variable = { CAN_subsidy_total > 0.1 }
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_reduce_infrastructure_subsidies"
			set_temp_variable = { subsidy_change_tmp = -0.1 }
			CAN_modify_subsidy_changes = yes
			custom_effect_tooltip = { localization_key = modifies_dynamic_modifier_tt MODIFIER = CAN_canadian_economy_dynamic_modifier }
			add_to_variable = { CAN_production_speed_infrastructure_factor_var = -0.05 tooltip = production_speed_infrastructure_factor_tt }
		}

		ai_will_do = { base = 1 }
	}
}

CAN_war_decisions = {
	CAN_burn_white_house = {
		icon = GFX_decision_generic_form_nation
		days_remove = 1

		available = {
			# Available if Washington DC is occupied
			controls_state = 1182
			NOT = { owns_state = 1182 }
		}

		fire_only_once = yes
		cost = 25

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove CAN_burn_white_house"
			ROOT = {
				add_war_support = 0.1
				add_timed_idea = {
					idea = CAN_white_house_burned
					days = 120
				}
			}
			hidden_effect = {
				USA = {
					add_stability = -0.15
					add_opinion_modifier = {
						target = CAN
						modifier = USA_WHITE_HOUSE_BURNED
					}
					add_timed_idea = {
						idea = USA_white_house_burned
						days = 180
					}
				}
				1182 = {
					if = {
						limit = { offices > 0 }
						remove_building = {
							type = offices
							level = 1
						}
					}
				}
				news_event = canada_news.2
			}
		}

		ai_will_do = { base = 1 }
	}
}

CAN_ai_path_category = {

	CAN_rally_the_nationalists = {

		icon = generic_decision

		cost = 25

		days_re_enable = 30

		visible = { has_global_flag = CAN_NATIONALIST_FOCUS_PATH }

		available = {
			has_civil_war = no
			check_variable = { party_pop_array^20 < 0.55 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision CAN_rally_the_nationalists"
			set_temp_variable = { party_index = 20 }
			set_temp_variable = { party_popularity_increase = 0.04 }
			set_temp_variable = { temp_outlook_increase = 0.04 }
			change_relative_party_popularity = yes
		}

		ai_will_do = { base = 100 }
	}
}
