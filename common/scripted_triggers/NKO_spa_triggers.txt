
# True when the SPA can run at all for this country.
NKO_spa_mechanic_enabled = {
	original_tag = NKO
	has_idea = NKO_spa_umbrella
	NOT = { has_civil_war = yes }
}

# True while fewer than 2 mandates are active — the concurrency cap. The
# counter starts at 0 in NKO history, is incremented on activation,
# and decremented when a bonus expires.
NKO_spa_picks_remaining = {
	check_variable = { NKO_spa_picks_made < 2 }
}
