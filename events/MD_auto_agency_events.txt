add_namespace = md_auto_agency

# Deferred update for the auto-agency button handlers — avoids same-tick
# processing that stripped queued items / started wrong upgrades.
country_event = {
	id = md_auto_agency.1
	hidden = yes
	is_triggered_only = yes
	immediate = {
		log = "[GetDateText]: [This.GetName]: md_auto_agency.1 executed"
		MD_auto_agency_update_state = yes
	}
}
