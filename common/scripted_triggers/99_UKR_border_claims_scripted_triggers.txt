UKR_border_claim_window = {
	OR = {
		has_civil_war = yes
		has_war_with = DPR
		has_war_with = LPR
		has_war_with = HPR
		has_war_with = OPR
		has_war_with = DRP
		has_war_with = SEU
		has_war_with = NOV
		has_capitulated = yes
	}
}
