faction_goal_seato_regional_stability = {
	name = seato_regional_stability_goal_name
	description = seato_regional_stability_goal_desc
	category = medium_term
	group = FOCUS_FILTER_POLITICAL_CHARACTER
	visible = {
		has_faction_research_unlocked = yes
	}
	completed = {
		num_faction_members > 4
		all_allied_country = {
			has_stability > 0.49
		}
	}
	complete_effect = {
		add_faction_power_projection = 300
		newline = yes
		custom_effect_tooltip = THE_FACTION_LEADER
		add_faction_initiative = 1
		newline = yes
		custom_effect_tooltip = ALL_FACTION_MEMBERS
		every_faction_member = {
			add_political_power = 200
			add_stability = 0.05
			add_timed_idea = { idea = SIA_seato_stability_dividend days = 730 }
		}
	}
	ai_will_do = {
		base = 1
		modifier = {
			add = 2
			has_stability > 0.6
		}
	}
}
