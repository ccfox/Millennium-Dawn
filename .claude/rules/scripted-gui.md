---
paths:
  - "interface/**"
  - "common/scripted_guis/**"
---

# Scripted GUIs

Read `.claude/docs/scripted-gui-rules.md` (engine mechanics: contexts, dirty counters, effect/trigger scopes) and `.claude/docs/scripted-gui-patterns.md` (MD recipes) before editing. GUI `window_name`, button names, and GFX sprite names are cross-referenced — grep all three before renaming anything.
