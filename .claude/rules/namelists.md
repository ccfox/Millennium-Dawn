---
paths:
  - "common/units/names_divisions/**"
  - "common/units/names_ships/**"
  - "common/units/names/**"
---

# Namelists

`division_types` entries must match `common/units/MD_land_units.txt` and `ship_types` must match `common/units/MD_naval_units.txt` exactly — case-sensitive, and removed vanilla tokens are silently dead. Canonical lists and templates: `.claude/docs/namelist-reference.md`.
