/** Full-resolution URL for the image lightbox (original under `/assets/images/...` or `public/`, not the responsive `currentSrc`). */
export const LIGHTBOX_FULL_SRC_ATTR = "data-lightbox-full-src" as const;
